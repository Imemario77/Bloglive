const CreateBlogReducer = (
  state = {
    blogData: null,
    loading: false,
    error: false,
  },
  action
) => {
  switch (action.type) {
    case Create_Blog_:
        
        break;
  
    default:
        break;
  }
};
